# CLAUDE.md — 福岡市テニスコート 空き照会ボット

このフォルダは、福岡市公共施設予約システムでテニスコートの「空き状況」を
自動で照会し、結果を見やすくまとめて公開するプロジェクトです。
Claude Code はこのファイルを最初に読み、以下のゴール・手順・制約を踏まえて
作業してください。

## コミュニケーション

- 日本語でやり取りする。
- 呼びかけは「とみーさん」。

## このボットのゴール

- 対象サイト: 福岡市 公共施設案内・予約システム
  `https://www3.11489.jp/fukuoka/user/Home`
- やること: 舞鶴公園・汐井公園のテニスコートの**空き状況を照会**し、
  空いているコマを施設ごとにまとめて HTML で出力する。
- **照会のみ。予約や申込はしない。ログインも不要**（＝利用者ID・
  パスワードは一切使わない）。

## 使う技術

- Node.js + Playwright（ブラウザ自動操作）。
- 定期実行と公開は GitHub Actions + GitHub Pages（後述）。

## 照会の条件

- 施設: **舞鶴公園** と **汐井公園** の2つ（両方まとめて照会）。
- 表示期間: **翌日から45日間**（実装上は「1ヶ月」＋「2週間」の2回照会を
  合算して約44〜45日をカバーしている。詳細は後述）。
- 表示曜日: **土曜日・日曜日・祝日**のみ。

## 操作手順（この順番で照会する。ログインはしない）

1. トップページを開く（`https://www3.11489.jp/fukuoka/user/Home`）
2. ログインせずに「空き照会・申込」または「利用目的から探す」へ進む
3. 「屋外スポーツ」を選択
4. 「テニス（屋外）」を選択
5. 「検索」をクリック
6. 「舞鶴公園」と「汐井公園」の両方にチェックを入れる
7. 「次へ進む」をクリック
8. 表示期間で「1ヶ月」を選択（翌日を起点にする）
9. 「＋その他の条件で絞り込む」→「土曜日」「日曜日」「祝日」にチェック
10. 「表示」をクリック
11. 空き状況グリッドから「空きあり（○）」のコマを、施設・日付ごとに読み取る

## 出力フォーマット

- 施設ごとに分けて、空いているコマを一覧にする。
- **隣り合う空き時間はつなげて範囲でまとめる**（例: 9:00と10:00が空きなら
  「9:00〜11:00（2時間）」とする）。
- 1行のイメージ:
  - `施設名：〇〇公園`
  - `mm月dd日（曜日）xx:xx〜xx:xx（x時間）`
  - `mm月dd日（曜日）xx:xx〜xx:xx（x時間）`
- 出力は HTML ページにする。スマホのブラウザで見やすいレイアウトにする
  （文字は大きめ、横スクロール不要、施設ごとに見出し）。
- ページには最終更新日時も表示する。

## 定期実行と公開

- **GitHub Actions** で1日10回、自動実行する。
  希望の実行時刻（日本時間）: **5:30 / 7:30 / 9:30 / 11:30 / 13:30 / 15:30 / 17:30 / 19:30 / 21:30 / 23:30**。
  ※GitHub Actions のスケジュールは UTC 指定なので、日本時間から9時間引いて
  　変換して書くこと（例: 5:30 JST → 20:30 UTC(前日)）。
- 生成した HTML を **GitHub Pages** に公開し、スマホのブラウザで開けるようにする。
- 定期実行は混雑時に多少ずれることがある前提でよい。

## tennis-bot からの流用

- 既存の tennis-bot プロジェクトにある「テニス（屋外）の画面まで進む
  ナビ部分」は流用してよい。
- ただし**ログイン処理と `.env` は今回不要**なので取り除く。GitHub 上に
  秘密情報を一切置かない構成にする。

## サーバーへの配慮・前提

- 照会のみ・1日5回なので負荷は軽いが、待機を適切に入れて丁寧にアクセスする。
- 短時間に何度も連打してテストしない。
- 自動アクセスの可否について、利用規約の確認は済んでいる前提で進める
  （未確認なら、先に確認するよう促してほしい）。

---

## 実際に操作して分かったこと（2026-09-16時点、実装時の調査より）

- **ログインなしで照会できることを確認済み**。トップページに最初から
  「利用目的から探す」タブがあり、ログインせずクリックできる。
- 画面遷移は2段階のグリッドになっている：
  1. **施設別空き状況**（`施設選択`→`次へ進む`後の画面、URLは
     `.../AvailabilityCheckApplySelectDays`）: 施設・日付ごとに1マスで、
     「空き」「一部空き」「空きなし」など**大まかな状況**しか分からない
     （コート・時間帯の区別はまだ無い）。ここで空き・一部空きのマスを
     選んで「次へ進む」を押すと、時間帯別空き状況に進む。
  2. **時間帯別空き状況**（URLは `.../AvailabilityCheckApplySelectTime`）:
     ここで初めてコート番号・1時間刻みの時間帯ごとの空き（`空きあり`
     ラベル、セルの親要素に `vacant` クラス）が分かる。
     → `src/steps/collectAvailability.js` の `parseTimeDetailPage`
- **選択可能数は最大10件まで**（施設別空き状況→時間帯別空き状況へ進む際、
  日付×施設の組み合わせで10件を超えると
  「選択可能数は、最大10件までです」というエラーダイアログが出る）。
  舞鶴公園・汐井公園を1ヶ月・土日祝で照会すると普通に10件を超えるため、
  10件ずつのバッチに分けて何度も往復する実装にしている。
- **「前に戻る」ボタンで施設別空き状況に戻っても、サーバー側には前バッチの
  選択状態が残っている**らしく、ブラウザ表示上はチェックが外れて見えても
  次のバッチを選ぶと合算されて「最大10件まで」エラーになった。
  対策として、2バッチ目以降は「前に戻る」を使わず、**毎回トップページから
  検索・施設選択・表示条件設定をやり直す**実装にした
  （`src/index.js` の `navigate()` を `collectAllAvailability` に渡し、
  バッチごとに呼び直している）。効率は落ちるが確実。
- 舞鶴公園は3コート（テニスコート（1番コート）〜（3番コート）、全角括弧）、
  **汐井公園は4コート**（テニスコート(テニスコート１)〜(テニスコート４)、
  半角括弧＋独特な表記）だった。施設別空き状況の段階では両施設とも
  「テニスコート」1行にまとまっていて面数は分からない。
- 時間帯別空き状況のセルには `TimeFrom`/`TimeTo` という hidden input が
  あり、値は「600」「1730」のような時分表記（600=6:00, 1730=17:30）。
  分単位に変換してから隣り合う空きコマをまとめている
  （`src/lib/formatSlots.js`）。
- 「表示」ボタンを押した直後、`table.table-schedule` 自体は既に
  DOM上に存在する（Ajaxで中身だけ後から更新される）ため、要素の存在確認
  だけで即座にマスを読み取ると古いデータを掴むことがある。
  1〜1.5秒程度の待機を入れている。
- `getByRole("button", { name: "表示" })` は、施設別空き状況グリッドの
  「表示範囲を右へ移動」ボタン（aria-label に「表示」を含む）と衝突して
  strict mode 違反になることがあるため `exact: true` を付けている。
- `.env` に本物の利用者ID・パスワードが平文で入った状態で見つかった
  （おそらく `tennis-bot`（抽選申込ボット）フォルダからのコピー時に
  紛れ込んだもの）。今回のボットはログイン不要なため `.env` ごと削除した。
  今後も万一 `.env` 的なファイルが混入していないか、コミット前に確認する。
- **「前回から増えた空き」機能**（2026-09-16追加）: 毎回の空きコマを
  `data/previous.json` にスナップショット保存し（`src/lib/snapshot.js`）、
  次回実行時と比較して増加分だけをページ上部に表示する
  （`src/lib/buildHtml.js` の `buildIncreaseSection`）。
  - 「前回もあった日付」かどうかの判定には、空き状況に関わらず
    施設別空き状況グリッドに表示された全 (施設, 日付) を
    `coveredDates` として記録している（`collectAvailability.js` の
    `listAllDates`）。表示期間は「翌日から1ヶ月」で毎日スライドするため、
    今回だけ新しく範囲に入ってきた日付（前回は照会対象外だった日）は
    比較対象から除外し、「増加」として誤検知しないようにしている。
  - 比較はコマ単位（施設・日付・コート・開始時刻・終了時刻）の完全一致で
    行い、表示時だけ従来どおり隣接コマをまとめる。
  - `data/previous.json` は GitHub Actions のコミット対象に含めている
    （`.github/workflows/check.yml` の `git add` に追加済み）。これを
    忘れるとスナップショットが毎回リセットされ、増加分が正しく出せない。
- **表示期間「45日間」への変更（2026-09-18追加）**: 施設別空き状況画面の
  「表示期間」欄（`SearchCondition.DisplayTerm`）は、実際にサイトを開いて
  確認したところ **「1日」「1週間」「2週間」「1ヶ月」の4プリセットのみ**
  で、任意日数や終了日の指定はできない。そのため「翌日から45日間」は
  1回の照会では作れず、`src/index.js` の `buildQueryWindows()` で
  「翌日から1ヶ月」＋「その1ヶ月後から2週間」の**2ウィンドウに分けて
  それぞれ最初から照会し直し、結果を合算**している（合計で約44〜45日分、
  月の日数により多少前後する）。各ウィンドウは内部で従来どおり
  `collectAllAvailability` が最大10件バッチに分けて処理する。
  ウィンドウ境界で日付が重複しても実害はないよう、`coveredDates` と
  `normalizedSlots` はどちらも合算後に重複除去している
  （`dedupeCoveredDates` / `dedupeNormalizedSlots`）。
  「正確に45日ちょうど」にするには最短でも6回照会（2週間×3＋1日×3）が
  必要になり、サーバーへの配慮の観点から見送った経緯がある。
- **空きコマ表示のコート表記変更（2026-09-18追加）**: 従来は
  `courtName`（例: 「テニスコート（1番コート）」「テニスコート
  (テニスコート１)」）をそのまま表示していたが、「テニスコート」という
  固定文言より施設名の方が分かりやすいため、`src/lib/buildHtml.js` の
  `courtDisplayLabel()` でカッコの中身だけを取り出し、施設名と組み合わせて
  「舞鶴公園（1番コート）」「汐井公園（テニスコート１）」のように表示する
  よう変更した。`courtName` 自体（`data/previous.json` に保存される生データ
  や前回比較のキー）は変更していない。

---

※これはたたき台です。実際にサイトを操作して分かったこと（空き表示の記号や
　画面の癖など）を、気づくたびにここへ追記していってください。
